
    const name = 'Aleksandr'
    const ageStr = 30
    const adress = `Pushkin str`
    const userInfo = `${name} from Moscow ${age} lives at ${address}`
    console.log(ageStr, `ageStr`)
    console.log(name, 'name')

    console.log(adress[0], `adress[1]`)
    console.log(adress.at(-1), `adress.at(-1)`)